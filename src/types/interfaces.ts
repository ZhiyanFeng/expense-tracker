export interface Expense {
    id: string;
    name: string;
    quantity: string;
    price: string;
    date: string;
}

export interface User {
    uid: string;
    firstName: string;
    lastName: string;
    email: string;
    photoUrl: string;
}
